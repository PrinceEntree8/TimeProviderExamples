<Window x:Class="TimeProviderExample.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:local="clr-namespace:TimeProviderExample.Wpf"
        mc:Ignorable="d"
        Topmost="True"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        d:DataContext="{d:DesignInstance Type=local:MainViewModel}"
        Title="UDP Time Display" Height="300" Width="600">
    <Window.Resources>
        <sys:Double x:Key="Double0.5">0.5</sys:Double>
        <sys:Double x:Key="Double1.0">1.0</sys:Double>
        <sys:Double x:Key="Double2.0">2.0</sys:Double>
        <sys:Double x:Key="Double4.0">4.0</sys:Double>
        <sys:Double x:Key="Double8.0">8.0</sys:Double>
    </Window.Resources>
    <Window.InputBindings>
        <KeyBinding Command="{Binding SpawnMainWindowCommand}" Key="F2" />
    </Window.InputBindings>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <Expander Grid.Row="0" Grid.Column="0" ExpandDirection="Right" IsExpanded="False" Background="#F0F0F0" BorderBrush="#CCCCCC" BorderThickness="0,0,1,0">
            <Expander.Header>
                <TextBlock Text="Time Control">
                    <TextBlock.LayoutTransform>
                        <RotateTransform Angle="270"/>
                    </TextBlock.LayoutTransform>
                </TextBlock>
            </Expander.Header>
            <StackPanel Margin="10" Width="200">
                <TextBlock Text="Time Scale" Margin="0,0,0,5" FontWeight="Bold"/>

                <UniformGrid Columns="2" Margin="0,0,0,5">
                    <Button Content="x0.5" Command="{Binding SetScaleCommand}" CommandParameter="{StaticResource Double0.5}" Margin="2"/>
                    <Button Content="x1" Command="{Binding SetScaleCommand}" CommandParameter="{StaticResource Double1.0}" Margin="2"/>
                    <Button Content="x2" Command="{Binding SetScaleCommand}" CommandParameter="{StaticResource Double2.0}" Margin="2"/>
                    <Button Content="x4" Command="{Binding SetScaleCommand}" CommandParameter="{StaticResource Double4.0}" Margin="2"/>
                    <Button Content="x8" Command="{Binding SetScaleCommand}" CommandParameter="{StaticResource Double8.0}" Margin="2"/>
                </UniformGrid>

                <DockPanel Margin="0,0,0,20">
                    <TextBlock Text="Custom:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox Text="{Binding Scale, UpdateSourceTrigger=LostFocus}" VerticalAlignment="Center"/>
                </DockPanel>

                <TextBlock Text="Set Date &amp; Time" Margin="0,0,0,5" FontWeight="Bold"/>
                <DatePicker SelectedDate="{Binding SelectedDate}" Margin="0,0,0,5"/>
                <TextBox Text="{Binding SelectedTime, StringFormat=\{0:hh\\:mm\\:ss\}, UpdateSourceTrigger=LostFocus}" Margin="0,0,0,20"/>
            </StackPanel>
        </Expander>

        <!-- Main Display -->
        <Grid Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel>
                <TextBlock Text="{Binding CurrentDate}"
                           FontSize="36" 
                           FontWeight="Bold" 
                           HorizontalAlignment="Center"
                           Margin="10"/>
                <TextBlock Text="{Binding CurrentTime}"
                           FontSize="48" 
                           FontWeight="Black" 
                           HorizontalAlignment="Center"
                           Margin="10"/>
            </StackPanel>
        </Grid>

        <!-- Status Bar -->
        <StatusBar Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Background="#DDDDDD">
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Margin="5,0">
                    <Run>System Clock: </Run>
                    <Bold>
                        <Run Text="{Binding RealTime}"/>
                    </Bold>
                </TextBlock>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
